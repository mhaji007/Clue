import React from 'react';
import Capture from '../Capture.PNG'

 const Landing = () => {
    return (
        <div style={{textAlign: 'center'}}>

            <img src={Capture} alt="Clue" style={{textAlign: 'center', marginTop:'12px'}}/>
        </div>

    );
};

export default Landing;